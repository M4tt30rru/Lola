##Import "std.lola"
##Find aggregate ##Expression(exp) s.t. ##Expression(filter) with ##Expression(id), ##Expression(red);
 ##replace (##(exp)).stream().filter(##(filter)).reduce(##(id), ##(red));

aggregate f() s.t. x -> x % 2 == 0 with 0, (x, y) -> x + y;
