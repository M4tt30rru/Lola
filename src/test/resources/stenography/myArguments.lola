##Import "std.lola"
##Find ##Type(t) ##Identifier(n)(##ArgumentDeclarationList) { ##Any(c1) (#) ##Any(c2) }
 ##replace ##(t) ##(n)(##(','.join([arg.t.name + ' ' + arg.n.name for arg in args]))){##(c1) (##(','.join(str(arg.n.name) for arg in args))) ##(c2)}

void main(Integer argc, String argv) {
  printf("Hello!");
  do_main_silently(#);
}
