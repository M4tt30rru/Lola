##Import "std.lola"
##Find ##Any(mods) ##CollectionType(col)<##Any(gen)> ##Identifier(n) = empty;
 ##replace ##(mods) ##(col)<##(gen)> ##(n) = new ##('HashSet' if col.name.strip() == 'Set' else 'HashMap' if col.name.strip() == 'Map' else 'LinkedList')<>();

private transient Map<X, Y> m = empty;
