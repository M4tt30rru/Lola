##Import "std.lola"
##Find
 ##Type(t) ##Identifier(n)(##ArgumentDeclarationList(l)) -> produce;
 ##replace ##(t) ##(n) (##(l)) {return new ##(t)(##(",".join(a.n.name for a in l.args)));}

T<String> f(int x) -> produce;
