##Import "std.lola"
##Find
 ##Type(t) ##Identifier(n)(##ArgumentDeclarationList(l)) -> ##Any(delegator);
 ##replace ##(t) ##(n) (##(l)) {##("" if t.name.strip() == "void" else "return") ##(delegator)(##(",".join(a.n.name for a in l.args)));}

public Coffee makeCoffee(Milk milk, List<Bean> beans) -> wife.makeIt;
public void takeTrash(Bag bag) -> husband.throwIt;
