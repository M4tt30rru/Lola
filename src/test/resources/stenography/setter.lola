##Import "std.lola"
##Find ##Any(prot) <- ##Any(mods) ##Type(t) ##Identifier(n);
 ##run {
setter = 'set' + n.name[0].upper() + n.name[1:]
 }
 ##replace ##(mods) ##(t) ##(n); ##(prot) void ##(setter) (##(t) __) {this.##(n) = __;}

protected <- private Integer i;
public <- protected transient String str;
